Создай интерактивный «Калькулятор оценок и симулятор СОР/СОЧ» для учеников в стиле темного iOS/Revolut интерфейса (как на прикрепленном референсе).
чтобы иконка его была между dash и reports
---

### 1. Архитектура и интеграция с данными:
1. **Загрузка предметов и оценок:**
   - Список предметов и четверти брать из текущей сессии через `useJournal()`.
   - При выборе предмета автоматически загружать реальные баллы из `useRubric(subjectId, quarter)`:
     - Все уже выставленные учителем СОРы (например, СОР 1: 8/10, СОР 2: 11/12) и СОЧ автоматически подставляются в калькулятор как стартовые значения.
   - Пользователь может свободно изменять любые значения, добавлять гипотетические СОРы или удалять их, чтобы смоделировать итоговый балл.
   - Кнопка **«Сбросить к реальным»** (иконка `Reset` сверху) возвращает исходные оценки из журнала.

---

### 2. Формула расчёта (Система оценивания НИШ):
- **СОР (50% от четверти):** 
  - Количество СОРов может быть любым (1, 2, 3 и более) с разными максимальными баллами.
  - $\text{Сумма СОР} = \frac{\sum \text{набранные баллы СОР}}{\sum \text{максимальные баллы СОР}} \times 50\%$.
- **СОЧ (50% от четверти):** 
  - $\text{Сумма СОЧ} = \frac{\text{набранный балл СОЧ}}{\text{максимальный балл СОЧ}} \times 50\%$.
- **Итоговый процент:** $\text{Итог} = \text{Сумма СОР} + \text{Сумма СОЧ}$.
- **Итоговая оценка:**
  - $85\% - 100\% \to 5$ (Отлично, зелёный цвет)
  - $65\% - 84.99\% \to 4$ (Хорошо, жёлтый/янтарный цвет)
  - $40\% - 64.99\% \to 3$ (Удовлетворительно, оранжевый/красный цвет)
  - $< 40\% \to 2$ (Неудовлетворительно, ярко-красный цвет)

---

### 3. Функция «Сколько нужно набрать на СОЧ до оценки»:
- Автоматический блок-подсказка под итоговой оценкой:
  - **До 5 (85%):** рассчитывает минимальный балл за СОЧ при текущих СОРах:
    $$\text{Балл СОЧ} = \left\lceil \frac{85 - \text{Сумма СОР}}{50} \times \text{Макс. балл СОЧ} \right\rceil$$
  - Если 5 уже гарантирована — писать: *«5 гарантирована при любом балле СОЧ 🎉»*.
  - Если на 5 уже не набрать математически — писать: *«Максимально возможный балл: X% (4)»*.
  - **До 4 (65%):** аналогичный расчёт минимального порога для удержания четвёрки.

---

### 4. Дизайн и UI (Строго по референсу с фото):
- **Тема:** Глубокий темный фон (`#0d0d0d` / `#121212`), мягкие скругленные карточки, акцентное теплое фоновое свечение снизу.
- **Верхний бар (Header):**
  - Слева кнопка закрытия `✕`.
  - По центру сегментная плашка-переключатель: выбор четверти (I, II, III, IV) или режима («Калькулятор» / «Цель»).
  - Справа кнопка `•••` (дополнительные действия / инфо о формуле).
- **Плашка выбора предмета (в стиле плашки Revolut на фото):**
  - Закругленная темная плашка с логотипом/иконкой предмета, названием (например, *«Алгебра»*) и текущим баллом из журнала (*«82% • 4»*). При клике открывается выбор другого предмета.
- **Главный экран (Hero Display по центру):**
  - Сверху мелким серым шрифтом формула: `42.5% СОР + 45.0% СОЧ`.
  - По центру **огромные цифры итогового процента и оценки**: `87.5%` с бейджем `5`.
  - Снизу кликабельная интерактивная плашка-подсказка со стрелочкой `>`:
    `🔄 Нужно 16/20 на СОЧ для 5 (85%) >`.
- **Список работ (СОР 1, СОР 2, СОЧ):**
  - Горизонтальный ряд интерактивных чипов/пилюль: `[ СОР 1: 8/10 ]`, `[ СОР 2: 12/12 ]`, `[ СОЧ: --/20 ]`, `[ + Добавить СОР ]`.
  - При нажатии на чип он становится активным для ввода через клавиатуру.
- **Кастомный цифровой блок (Numpad снизу):**
  - Темные кнопки с закругленными углами (`7 8 9 /`, `4 5 6 *`, `1 2 3 -`, `, 0 ⌫ +`).
  - Быстрые кнопки над клавиатурой: иконка сброса `↺`, кнопка «Максимум» (заполнить 100%), переключатель набранного/максимального балла.
- **Нижняя панель (Action Bar):**
  - Слева пилюля активного поля (например, `🟠 СОР 1`).
  - Справа кнопка «Применить» / «Сохранить» в контрастном белом/светлом оформлении.